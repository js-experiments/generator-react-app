language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-app' ]; then cd .. && eval "mv $currentfolder generator-react-app" && cd generator-react-app; fi

